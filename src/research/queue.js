export function researchTriggers(player,knowledge,{now=Date.now()}={}){const out=[];if(!knowledge)out.push('unknown-player');if(Number(player.minutes||0)<180 && !knowledge?.prior)out.push('insufficient-pl-data');if(['d','i','u','s'].includes(player.status)&&(!knowledge?.availabilityCheckedAt||now-Date.parse(knowledge.availabilityCheckedAt)>12*3600e3))out.push('availability-change');if(knowledge?.clubId!=null&&player.teamId!=null&&knowledge.clubId!==player.teamId)out.push('club-change');return [...new Set(out)];}
export function buildResearchQueue(players,store,season,opts={}){return players.flatMap(p=>{const k=store.get(season,p.id),triggers=researchTriggers(p,k,opts);return triggers.length?[{season,playerId:p.id,name:p.name,team:p.team,triggers,priority:triggers.includes('unknown-player')?100:50}]:[];}).sort((a,b)=>b.priority-a.priority);}
