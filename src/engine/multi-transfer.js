import {sellingPrice} from './selling-price.js';import {transferHitCost,nextFreeTransfers} from './transfer-cost.js';import {optimiseLineup} from './lineup.js';
const horizonScore=(squad,gws,discount=.9)=>gws.reduce((sum,gw,i)=>{const l=optimiseLineup(squad,p=>p.projections?.[gw]||0);const cap=l.captain?.projections?.[gw]||0;return sum+Math.pow(discount,i)*(l.value+cap)},0);
const signature=s=>s.map(p=>p.id).sort((a,b)=>a-b).join(',');
export function generateTransferPlans(state,allPlayers,gws,{maxTransfers=5,beamWidth=180,clubLimit=3,hitPoints=4}={}){const owned0=new Set(state.squad.map(p=>p.id));let beam=[{squad:state.squad,bankTenths:state.bankTenths,moves:[]}],complete=[];const baseline=horizonScore(state.squad,gws);
 const playerHorizon=p=>gws.reduce((s,g,i)=>s+Math.pow(.9,i)*(p.projections?.[g]||0),0);const candidatesByPos={};for(const pos of ['GK','DEF','MID','FWD'])candidatesByPos[pos]=allPlayers.filter(p=>p.position===pos).sort((a,b)=>playerHorizon(b)-playerHorizon(a)).slice(0,35);
 for(let depth=0;depth<=maxTransfers;depth++){for(const st of beam){const hit=transferHitCost(st.moves.length,state.freeTransfers,hitPoints);complete.push({...st,hit,netGain:horizonScore(st.squad,gws)-baseline-hit,nextFT:nextFreeTransfers(state.freeTransfers,st.moves.length)});}if(depth===maxTransfers)break;const next=[],seen=new Set();for(const st of beam){const owned=new Set(st.squad.map(p=>p.id));const clubs=new Map();for(const p of st.squad)clubs.set(p.teamId,(clubs.get(p.teamId)||0)+1);for(const old of st.squad){if(st.moves.some(m=>m.out.id===old.id))continue;const sale=sellingPrice(old.purchasePriceTenths??old.priceTenths,old.priceTenths);for(const p of candidatesByPos[old.position]){if(owned.has(p.id))continue;const newClubCount=(clubs.get(p.teamId)||0)+(p.teamId===old.teamId?0:1);if(newClubCount>clubLimit)continue;const funds=st.bankTenths+sale;if(p.priceTenths>funds)continue;const squad=st.squad.map(x=>x.id===old.id?p:x);const sig=signature(squad);if(seen.has(sig))continue;seen.add(sig);next.push({squad,bankTenths:funds-p.priceTenths,moves:[...st.moves,{out:old,in:p}]});}}}next.sort((a,b)=>horizonScore(b.squad,gws)-horizonScore(a.squad,gws));beam=next.slice(0,beamWidth);if(!beam.length)break;}
 const uniq=new Map();for(const x of complete){const k=signature(x.squad);if(!uniq.has(k)||uniq.get(k).netGain<x.netGain)uniq.set(k,x);}return [...uniq.values()].sort((a,b)=>b.netGain-a.netGain);}
